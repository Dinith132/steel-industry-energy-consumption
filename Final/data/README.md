# Data

Primary CSV (relative to repo root):

`../steel-industry-energy-consumption/steel_industry_data_preprocessed_v1.csv`

Raw fallback:

`../steel-industry-energy-consumption/Steel_industry_data.csv`

Preprocessed outputs are written to `../outputs/{track}/` (not committed).
